csv parser for Lua
based on my lua-ext stuff

example usage:

d = csv.file(filename)
d = csv.string(string)

d.rows[i] = i'th row
d.rows[i][j] = cell i,j

names = {'id', 'rank', 'etc'}
d:setColumnNames(names) --> sets column names, then you can do this:
d.rows[1].rank		--> gets you cell 1,1


